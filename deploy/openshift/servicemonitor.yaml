apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: headroom-gateway
  labels:
    app: headroom-gateway
spec:
  selector:
    matchLabels:
      app: headroom-gateway
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
